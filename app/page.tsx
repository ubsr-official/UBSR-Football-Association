import Link from "next/link";
import { ArrowRight, CalendarDays, Gavel, ShieldCheck } from "lucide-react";
import { getLeagueOverview } from "@/lib/league";

export default async function LandingPage() {
  const overview = await getLeagueOverview();
  return <main className="landing"><header className="topline"><div className="wordmark"><span className="mark">U</span><span>UFA League</span></div><Link className="button button-secondary" href="/login">Member sign in</Link></header><section className="hero"><div><p className="eyebrow">United Boys Socialist Republic Football Association</p><h1>Football, run with intent.</h1><p className="lede">A secure home for the ongoing auction, league squads, manager-led fixture matching, match logs, and the official UFA League table.</p><div style={{ display: "flex", gap: ".75rem", flexWrap: "wrap", marginTop: "1.5rem" }}><Link className="button" href="/login">Enter the league <ArrowRight size={16} style={{ verticalAlign: "-3px", marginLeft: ".35rem" }} /></Link><span className="status closed"><Gavel size={14} /> Transfer market closed</span></div></div><aside className="hero-card"><p className="eyebrow">League record</p><div className="metric-grid"><div className="metric"><strong>{overview.memberCount}</strong><span>registered members</span></div><div className="metric"><strong>{overview.teamCount}</strong><span>manager teams</span></div><div className="metric"><strong>{overview.auctionCounts.sold}</strong><span>auctioned players</span></div><div className="metric"><strong>{overview.auctionCounts.unsold + overview.auctionCounts.notCalled}</strong><span>awaiting outcome</span></div></div><div className="notice"><ShieldCheck size={15} style={{ verticalAlign: "-3px", marginRight: ".35rem" }} />Two-administrator market safeguard enabled.</div><p style={{ display: "flex", gap: ".45rem", alignItems: "center", marginTop: "1rem", fontSize: ".86rem" }}><CalendarDays size={16} />Fixtures are scheduled by shared available day, then approved by administrators.</p></aside></section><footer style={{ color: "var(--muted)", fontSize: ".78rem" }}>UBSR FA League · UFA League · UFL</footer></main>;
}
