import { Standing } from "@/lib/league";

export function StandingsTable({ standings }: { standings: Standing[] }) {
  return <div className="table-wrap"><table className="table"><thead><tr><th>#</th><th>Team</th><th className="number">P</th><th className="number">W</th><th className="number">D</th><th className="number">L</th><th className="number">GF</th><th className="number">GA</th><th className="number">GD</th><th className="number">Pts</th></tr></thead><tbody>{standings.length ? standings.map((team, index) => <tr key={team.team_id}><td>{index + 1}</td><td><strong>{team.team_name}</strong></td><td className="number">{team.played}</td><td className="number">{team.wins}</td><td className="number">{team.draws}</td><td className="number">{team.losses}</td><td className="number">{team.goals_for}</td><td className="number">{team.goals_against}</td><td className="number">{team.goal_difference > 0 ? `+${team.goal_difference}` : team.goal_difference}</td><td className="number"><strong>{team.points}</strong></td></tr>) : <tr><td colSpan={10}><div className="empty">The competitive table will appear after the first administrator-confirmed result. Friendlies are intentionally excluded.</div></td></tr>}</tbody></table></div>;
}
